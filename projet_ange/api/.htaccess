RewriteEngine On
# Passe le header Authorization à PHP (Apache le supprime par défaut)
RewriteCond %{HTTP:Authorization} ^(.*)
RewriteRule .* - [E=HTTP_AUTHORIZATION:%1]
SetEnvIfNoCase Authorization "(.*)" HTTP_AUTHORIZATION=$1
